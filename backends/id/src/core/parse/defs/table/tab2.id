// Append a new definition's default colours (-1 = none), then let the property
// scan (hand.id) overwrite the last row's bg/fg as it reads `bg:`/`fg:` lines.
def_add2() {
  push((import dbg), 0 - 1);
  push((import dfg), 0 - 1);
} return void;

set_last_bg(int c) {
  int idx = len((import dbg)) - 1;
  lset((import dbg), idx, c);
} return void;

set_last_fg(int c) {
  int idx = len((import dfg)) - 1;
  lset((import dfg), idx, c);
} return void;
