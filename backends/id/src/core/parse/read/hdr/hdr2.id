// Read the two leading numbers of a `[h,w,align]` dimension list.
read_two(int dir) {
  adv();
  read_two_tail(dir);
} return void;

read_two_tail(int dir) {
  int p = pos() + 0;
  string t = text_at(p);
  read_two_tail2(dir, t);
} return void;
