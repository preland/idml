
px_idx(int x, int y) {
  int result = y * (import gw) + x;
} return int result;

pset(int x, int y, int c) {
  if(x >= 0 && x < (import gw) && y >= 0 && y < (import gh)) {
    int idx = px_idx(x, y);
    lset((import fb), idx, c);
  }
} return void;
