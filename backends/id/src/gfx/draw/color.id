// Pack an (r,g,b) triple (each 0..255) into one 0xRRGGBB pixel via plain
// arithmetic (id has no bitwise ops), the same packing demos/gfxdemo uses.

rgb(int r, int g, int b) {
} return int r * 65536 + g * 256 + b;
