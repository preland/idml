// Draw a string left-to-right at (x,y) in colour c, magnified `scale`x. Each
// glyph cell is 8*scale px wide, so characters advance by that. text_width gives
// the pixel width of a string so callers can right-align or centre it.

draw_text(int x, int y, string s, int c, int scale) {
  int i = 0;
  while(i < len(s)) {
    int ch = charat(s, i);
    draw_glyph(x + i * 8 * scale, y, ch, c, scale);
    i = i + 1;
  }
} return void;

text_width(string s, int scale) {
  int result = len(s) * 8 * scale;
} return int result;
