// String helpers. slice(s,a,b) is the substring s[a:b] (id has no substring
// builtin, so build it a char at a time).
slice(string s, int a, int b) {
  string out = "";
  int i = a;
  while(i < b) {
    int c = charat(s, i);
    out = out + chr(c);
    i = i + 1;
  }
} return string out;

