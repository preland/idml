# Changelog

## 0.0.1

- Initial release: TextMate grammar + language configuration for `.idml`.
- Highlights routes, components, layout primitives, styled-variant defs,
  value/model bindings, handlers, dimension blocks, class blocks, inline style
  blocks, strings, hex colours, and header comments.
